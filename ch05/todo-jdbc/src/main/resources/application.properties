spring.application.name=todo-jdbc
spring.h2.console.enabled=true
# Overriding the default url to access to the H2 console
spring.datasource.url=jdbc:h2:mem:testdb
# Setting to visualize the SQL statements executed in H2 console   TODO: Needs to ensure functionality
logging.level.org.springframework.data=INFO
logging.level.org.springframework.jdbc.core.JdbcTemplate=DEBUG
